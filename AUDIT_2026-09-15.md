# Comic sorting 全專案審查（2026-09-15）

## 範圍與專案現況

審查基準為本機 `74cae31`，開始時工作目錄乾淨。範圍是本專案 13 個正式 Python 模組、15 個測試檔、建置規格、兩個 CI workflow 及使用文件。依實際呼叫關係檢查掃描 → 整合 → 翻譯 → 完整性檢查 → 匯出 → 清理，以及狀態保存 → 歷史查詢 → 通知流程；另核對先前的改善計畫與 9/12、9/13 審查記錄。外部 BallonsTranslator 是獨立應用，僅查介接契約，未改動其程式或執行付費模型。

使用者是 Windows 漫畫收藏者；核心需求是批次整合章節、呼叫已安裝的翻譯器、匯出 Komga CBZ、保存用量與失敗原因，並發送跨裝置通知。

| 責任 | 實作與資料流 |
|---|---|
| 入口、漫畫列表與整合 | `Comic sorting.py`：Tkinter 主視窗、掃描與整合背景工作、搜尋排序、多選、直接匯出、視窗設定 |
| 檔案操作 | `comic_core.py`：自然排序、翻譯完整性、來源指紋、CBZ 驗證／原子替換、JSON 保存、清理；`windows_recycle.py`：Windows 回收筒 |
| 佇列 | `translation_queue.py` 保存 Job、接收事件更新 UI；`queue_worker.py` 依序執行資料夾，隔離單項失敗，發出進度／用量／耗時事件 |
| 翻譯器介接 | `bt_settings.py` 編輯設定；`bt_config_bridge.py` 在翻譯器 Python 環境讀取定義與驗證；`bt_run_bridge.py` 傳送選定頁面 |
| 歷史 | `queue_history.py`：每批一個 SQLite row，JSON 保存每資料夾明細；查詢唯讀，最多 200 筆；費用保留 Decimal 原值，顯示才向上取兩位 |
| 通知 | `ntfy_notifications.py`：獨立有限容量背景佇列，HTTP 發布，有限重試，Windows 使用者加密權杖；不阻塞翻譯 |
| 共用 | `ui_language.py` 三語字串（重啟套用）；`app_logging.py` 輪替日誌與常見密鑰遮蔽；`queue_errors.py` 錯誤分類 |

設定與佇列保存在 EXE 旁的 JSON；歷史是同目錄 SQLite；Komga 目錄另有增量狀態 JSON。掃描結果只在記憶體快取，匯出指紋使用名稱、大小及修改時間。檔案工作由單一佇列執行，模型內部平行處理由外部翻譯器負責。正常 UI 事件只在主執行緒套用，翻譯事件每輪最多處理 100 筆。

優點是 UI、執行器、檔案邏輯與歷史已有明確界線，已有原子保存、備份、路徑限制、失敗隔離、回收筒和離線回歸測試。最大技術債是外部翻譯器的日誌／程序生命週期契約，以及多個程式實例同時寫入同一目錄時沒有跨程序交易。沒有足夠收益支持更換 GUI、資料層或新增通用服務架構。

## 修改前的優先級計畫

下表在正式程式修改前建立。P0：本次未確認必須立即停用整個程式的漏洞或毀損；這不代表所有環境均無問題。P1 優先處理已重現的完整性與漏報，P2 處理有量測的改善及文件。

| 項目 | 分類 | 問題 | 影響 | 修改方式 | 風險 | 工作量 | 優先級 |
|---|---|---|---|---|---|---|---|
| A | 正確性／可靠性 | `1.png`、`1.jpg` 兩原圖僅有一張結果仍被 set(stem) 判為完整 | 已重現翻譯標完成、建立不完整 CBZ 並清掉 mask | 共用完整性判斷；重複 basename 要求可區分的完整檔名，單一 basename 保留副檔名轉換相容性；套用全章及指定頁面 | 低；含歧義的舊結果會改列不完整 | 小 | P1 |
| B | 掃描／錯誤回報 | `os.walk` 預設吞掉子目錄讀取錯誤；模擬 PermissionError 後回傳空列表 | 未掃完卻顯示成功，清單遺漏章節 | 使用現有 worker 錯誤事件；掃描失敗保留前次清單及可操作狀態 | 低；不能讀的目錄改為明確失敗 | 小 | P1 |
| C | DB／效能 | 最新歷史查詢無排序索引 | 5 萬筆合成資料查最近 200 筆 median 22.57 ms，全表掃描加暫存排序 | 儲存時建立 started_at 索引，保持既有 DB 格式與唯讀查詢 | 低；首次建立需寫入索引 | 小 | P2 |
| D | 文件／可維護性 | README 仍寫三分頁、固定保留最後一個，模組清單遺漏新增功能 | 首次使用及交接理解不一致 | 更新實際分頁、獨立清理選項、模組清單及本輪限制 | 低 | 小 | P2 |
| E | 取消／程序生命週期 | 停止只針對直接子程序，孫程序可能保留 stdout 管線 | 停止到結束可能延遲；先前 synthetic 已量到此現象，本輪重新查閱該路徑 | 未來以實際 BT 程序樹重現，再評估 Windows Job Object | 中高；錯誤終止可能影響非本工作模型程序 | 中 | P1（待外部重現） |
| F | 多實例／一致性 | 同檔 JSON 最後保存者覆蓋，整合暫存目錄固定 | 多個 EXE 操作同一資料會互相干擾 | 維持文件單實例限制；有多實例需求時增加明確鎖定與衝突流程 | 中；涉及復原與鎖範圍 | 中 | P2 |
| G | UI／大型歷史 | 匯出／整合事件未設每輪上限，歷史展開資料一次建立 | 極大量事件或每批極多工作可能卡 UI；本輪尚無使用者尺度量測證據 | 取得卡頓 trace 後再加批次插入或延後明細載入 | 低至中 | 中 | P2（待量測） |
| H | 開發流程 | 本地 Python 3.10.6 與 CI 的最新 3.10 patch 不完全一致，建置間接依賴未全部鎖版 | 位元級建置重現性有限 | 需要可重現供應鏈時記錄工具鏈並鎖定全依賴 | 低至中 | 中 | P2 |
| I | 架構／大型功能 | 主視窗 1020 行、佇列 UI 782 行；大部分長函式為建構 widget | 局部閱讀成本較高，尚未證實獨立服務／框架更換有益 | 保留現有模組，新增功能時依職責局部拆分；不全面重寫 | 大重構風險高 | 大 | P3 |
| J | 產品功能 | 通知不持久重送、掃描／直接匯出不可取消、歷史無分頁 | 程式關閉後通知可能未送達，超過 200 筆需縮小查詢範圍 | 有明確使用需求才加入持久 outbox、可復原取消、分頁 | 中 | 中至大 | P3 |

執行順序 A → B → C → D。每個程式修改先留下可失敗的最小回歸案例，再跑相關測試、語法檢查及打包；最後跑完整測試並審查完整差異。測試只使用暫存合成漫畫，不修改真實漫畫、不重送真實通知、不啟動付費翻譯。

## 20 個審查面向

| 面向 | 實際檢查與判斷 |
|---|---|
| 1 架構 | 追蹤上述主流程與橋接邊界，AST import graph 檢查 13 個本地模組無循環。大型 UI 保留；A 的重複判斷收斂到現有 core 模組。 |
| 2 程式碼品質 | 檢查 13 模組的 import／函式及直接呼叫。長函式主要是 widget 建構、run_jobs 與 poll；未以行數或格式為由大改，避免混入無關刪改。 |
| 3 正確性 | 檢查空路徑、重疊整合、同名來源、指定頁面、部分結果、零元／缺報、日期、重試與狀態保存；A 為已確認遺漏邊界。 |
| 4 效能 | 檔案以 scandir、自然排序與增量指紋處理，CBZ 採 ZIP_STORED 避免重壓圖片；C 有 before/after query plan 和合成量測。未宣稱 CPU／RAM 或模型速度改善。 |
| 5 UI／UX | 檢查摺疊預設、上下區域尺寸、Ctrl/Shift 多選、搜尋防抖、排序、失敗原因／錯誤碼、busy 按鈕及進度。B 防止錯誤空清單；G 留待量測。 |
| 6 首次／日常使用 | 路徑初值空白，瀏覽／保存／原生設定入口齊全；移動環境有路徑解析。檢查重啟還原、重試及合併僅入列。D 修正文案。 |
| 7 功能設計 | 匯出、清理、保留最後一個與完成開資料夾為不同選項；歷史 batch/folder/stage 層級合理。J 是功能取捨，非本輪追加功能。 |
| 8 可靠性 | 檢查 CBZ 驗證、原子替換、JSON strict load、備份／存檔衝突、歷史 checkpoint、同路徑前置失敗阻擋後續；A 避免不完整結果清理。E/F 保留限制。 |
| 9 錯誤處理 | 檢查子程序退出碼不等同成功、完成標記／各階段／輸出檢查、分類碼與續跑下一項、掃描 error 事件；B 修正吞錯誤。 |
| 10 日誌 | UTF-8 輪替 10 MB × 主檔及 3 備份，run/job ID、退出碼、分類碼；常見 key/token/密碼遮蔽。仍含漫畫路徑及翻譯器文字，分享需檢查，不能保證任意文字皆可識別為秘密。 |
| 11 安全性 | 清理與回收筒限制根目錄、拒絕連結／junction、先驗證遞迴子樹；子程序使用參數陣列。ntfy 權杖 HTTPS、加密保存、拒絕重新導向、錯誤不回顯秘密；未執行外部滲透測試。 |
| 12 並行 | 檢查主 UI 事件、單一檔案佇列、bounded ntfy 工作佇列、停止事件與儲存 checkpoint。沒有新增模型平行處理；E/F 為邊界風險。 |
| 13 DB／資料模型 | SQLite 每批 JSON 快照、id upsert、交易與關閉連線；唯讀查詢、參數化 SQL；C 加排序索引。keyword 是子字串，非全文索引；SQLite lower 的 Unicode 大小寫能力有限。 |
| 14 API／介接 | ntfy 發布檢查回應、10 秒等待、429/5xx 最多一次重試，不將代理 403 武斷歸因權杖；BT stdout/schema 屬版本契約，升級須 fixture 驗證。 |
| 15 依賴 | 執行期僅直接依賴 pywin32 312（Windows）；Python 標準庫完成 UI/ZIP/HTTP/DB，PyInstaller 6.22.2 只供打包。未增加套件或盲目升版；見下方官方來源及檢查限制。 |
| 16 建置／開發 | Verify 使用 contents:read，Windows/Python 3.10 跑 unittest＋PyInstaller；Release 僅 v* 標籤使用寫入權限。spec 收入圖示與兩個 bridge。H 記錄重現性限制。 |
| 17 測試 | 基準為 181 個 unittest；涵蓋 Tk 事件、故障注入、檔案保存、翻譯 stub、通知本地 HTTP server。新增真實遺漏案例，未增加測試框架；最終結果另列。 |
| 18 文件 | 閱讀 README、ERROR_CODES、三份既有改善／審查資料與 workflow；D 修正 README 與實作不一致。過往報告保留為歷史。 |
| 19 可維護性 | 檔案、worker、UI 已可分開測試；語言 catalog 與錯誤分類共用。保持流程直觀，A 只新增一個有多個實際呼叫者的判斷函式。 |
| 20 技術債 | E–J 列出原因、目前／未來影響、成本、風險與觸發修改條件。另保留來源指紋非內容雜湊、結果不解碼驗證翻譯品質的已知上限。 |

## 效能基線與依賴證據

同一 Windows 主機、Python 3.10.6／SQLite、暫存 DB 5 萬筆相同形狀紀錄，7 次查詢中位數：

| 查詢 | 修改前 | 索引實驗後 |
|---|---:|---:|
| 最新 200 筆（只計 SQL fetch，含 JSON `jobs:[]` 字串） | 22.5706 ms | 0.1453 ms |
| query plan | SCAN runs + USE TEMP B-TREE FOR ORDER BY | SCAN runs USING INDEX runs_started_at |

此量測是合成資料上的 SQL 改善，不能推論整個歷史視窗快 155 倍；只有五筆歷史時收益微小，JSON 解析及 Tk 建立未計入。另一輪同樣 5 萬筆資料實驗，首次建立索引耗時 21.59 ms，DB 增加 1,687,552 bytes；已索引 DB 更新一筆並 commit 的 7 次中位數為 1.76 ms。既有歷史不因查詢而寫入；索引會於下次保存佇列建立。

完整性判斷的成本也有量測：1000 張 `.jpg` 原圖對應 `.png` 結果，純函式 7 次中位數由 0.650 ms 增為 1.749 ms。增加約 1.1 ms 是辨識重複 basename 所需的線性檢查，不宣稱此項加速；未引入持久快取或額外檔案讀取。

已透過 Context7 核對 [PyInstaller 官方 changelog](https://pyinstaller.org/en/stable/CHANGES.html)：6.22.2 修正 Windows onefile 從 symlink／junction 啟動時的誤報安全驗證。本機與 CI 都使用 6.22.2，因此本輪不以猜測升級套件。本次不是完整套件供應鏈／CVE 掃描，未宣稱所有間接依賴沒有漏洞。

## 實作與最終驗證

### A：結果完整性（已完成）

原始重現：`1.png`／`1.jpg` 兩張不同原圖，只有 `result/1.png`；原程式顯示「可匯出」，佇列狀態為 running → done，`mask/keep.png` 被清除。根因是兩處使用不保留重複數量的 basename set。

在 `comic_core.py` 新增共用 `translations_complete`，`translation_status` 與 `queue_worker.py` 的指定頁面檢查都呼叫它。只有 basename 唯一時允許副檔名變更；重複 basename 的頁面需要完整檔名相符的結果。沿用既有「部分翻譯」「翻譯結果不完整」錯誤與續跑機制，未另加狀態格式。

新增測試先於舊程式失敗（可匯出 ≠ 部分翻譯），再驗證全章／指定重複頁面都失敗、無 CBZ、mask 保留；補齊兩張結果可匯出，單一 basename 的 jpeg → webp 也通過。相關 24 項檔案與頁面範圍測試、compileall、PyInstaller 建置通過。完整 suite 另涵蓋 result-only、大小寫 result 目錄、junction 及一般部分頁面完成後不清理。

### B：掃描讀取失敗（已完成）

根因是 `get_folders_with_numbers` 的 `os.walk` 未指定 `onerror`。以真實暫存目錄加 PermissionError 注入，修改前漏掉整個系列且回傳空列表，Tk 不顯示錯誤。

`Comic sorting.py` 使用現有的 raise-on-error 慣例，讓原來的 worker／UI error 分支接手。回歸驗證顯示讀取錯誤與路徑、保留舊樹列、解除 busy，不需新增 UI 分支。測試在舊程式先因沒有 showerror 失敗；修正後曾發現測試對 Windows OSError 路徑 repr 的期待錯誤，調整測試後，67 項 improvements／comic_sorting 測試與語法檢查、打包全部通過。

### C：歷史排序索引（已完成）

根因是最新 200 筆排序欄位沒有索引。`queue_history.py` 僅在既有儲存 transaction 路徑加 `CREATE INDEX IF NOT EXISTS runs_started_at ON runs(started_at)`；無新 migration 框架、不修改 JSON schema、不將唯讀查詢變成寫入。

回歸從舊表格式建立 205 筆含相同開始時間的紀錄，先確認查詢不改寫檔案；下次保存後排序與最新 200 筆內容一致。舊程式 query plan 測試先失敗；修正後確實使用索引且沒有暫存排序。歷史／用量 8 項測試、語法檢查與打包通過。效能與索引成本見上表。

### D：文件與交接（已完成）

`README.md` 更正四分頁、獨立保留來源選項，補上完整性規則、掃描失敗時保留清單、歷史索引與新增模組責任。新增本報告，保留舊報告作為歷史，沒有將先前已完成的 ntfy／部分費用修正算成本輪新修正。

### 全部驗證結果

- 基準 181 項，本輪新增 3 個有實際失敗重現的測試：**184 項 unittest 全部通過，42.111 秒**。
- 正式模組與測試共 **28 個 Python 檔案 py_compile 通過**；本專案沒有設定獨立 lint／靜態 type checker，未冒稱已執行。
- `git diff --check` 通過；Git 提示的 LF → CRLF 為現有 Windows 換行設定，不是測試失敗。
- 三個程式修改單元皆成功以 **Python 3.10.6 + PyInstaller 6.22.2** 打包。檢查 archive 已含 core、queue/history、ntfy、兩個 bridge、win32crypt 與 pythoncom；warning 清單主要是其他平台／選用模組，未見本輪正式模組遺漏。
- 本輪有實際 Tk UI 測試與 stub 子程序整合測試；沒有啟動新 EXE 操作真實漫畫、沒有執行付費 OCR／翻譯、沒有重新發送真實 ntfy 通知。新 EXE 的手動啟動及真實批次仍屬執行環境驗證限制。
- 本地證據：`build/audit-20260915-A-tests.log`、`-B-tests.log`、`-C-tests.log`、`build/audit-20260915-tests.log` 與相同前綴 build logs；均不提交到 Git。

## Standards

使用 code-review 技能直接審查，固定比較命令 `git diff 74cae31 -- "Comic sorting.py" comic_core.py queue_worker.py queue_history.py tests/test_improvements.py tests/test_queue_history.py README.md`，另讀新報告。依使用者 AGENTS 規範確認只改相關流程、保留架構／命名、無新依賴、無遠端寫入、每項有最小回歸。新共用函式有全章與指定頁面兩個實際用途；沒有單一用途工廠或新服務層。**需修正的規範違反：0**。全量建置與測試資料、密鑰及使用者設定不加入提交。

## Spec

規格來源為本次目標的 pasted-text-1.txt。確認完成：專案理解、20 面向審查、修改前優先級表、A → B → C → D 分段驗證、修改後審查、實測及未修改原因。共享完整性涵蓋掃描／直接匯出／佇列全章與指定頁；索引不改變歷史金額或排序；掃描錯誤沿用既有 UI 恢復路徑。**本輪選定修正的需求缺漏：0**。下列高風險／未量測技術債依規格允許明確延後，不視為已修正。

後審查結果：Standards 0 項；Spec 0 項；沒有需另行修正的最嚴重項目。此結論只針對本輪差異，不代表整個系統永無問題。

## 尚未修改與真正有價值的後續工作

1. **E：程序樹取消**。需真實 BT 孫程序／管線 trace；目前修正可能連帶終止仍在寫檔的模型程序。先在隔離環境驗證程序所屬關係與清理策略，再引入 Windows Job Object，不以程序名稱全域強殺。成本中、相容風險中高。
2. **F：多實例協調**。原子替換只能保證單次寫入完整，不能合併兩個 EXE 的設定快照；固定整合暫存名稱也存在競爭。目前維持 README 的一次一個實例限制。有多實例需求時，先定義設定與漫畫目錄各自的鎖定範圍、唯讀模式及中斷復原，再實作。成本中、風險中。
3. **G/H：有量測再擴充**。若大量歷史或事件確實造成卡頓，記錄一次 Tk 主執行緒耗時後再分批插入；需要位元級重現建置時才鎖定 Python patch 與間接依賴。未藉本輪升級 GUI 或套件。
4. **I/J：保留合理簡化**。不用大型 MVC 重寫；不新增尚無明確需求的歷史分頁、持久通知重送或複雜取消工作流。通知最多只保證伺服器接收，未送完便關閉不會保證補送；這些是已有文件的限制。
5. **結果內容／外部版本**。本輪檢查檔名映射，未解碼所有圖片確認品質或重新計算所有內容雜湊；同名舊結果、外部工具並行改檔仍需操作規範。BT 日誌、私有 schema 或 page hook 更新時，先保存去識別的失敗／完成樣本做相容性 fixture，再變更判斷；不把任何 error 字樣一律視為成功。

## 修改檔案與本機交付

| 檔案 | 內容 |
|---|---|
| `comic_core.py`、`queue_worker.py` | 共用結果完整性，避免重複 basename 少頁誤判完成 |
| `Comic sorting.py` | 掃描子目錄錯誤向既有錯誤事件傳遞 |
| `queue_history.py` | 歷史開始時間排序索引 |
| `tests/test_improvements.py` | 同名頁面全章／範圍、後續資料保留及真 Tk 掃描錯誤回歸 |
| `tests/test_queue_history.py` | 舊 DB 索引建立、唯讀查詢及 200 筆排序保持 |
| `README.md`、`AUDIT_2026-09-15.md` | 使用說明與本次分析、計畫、驗證及限制 |

新版：`C:\Users\666dd\Documents\application\Comic-sorting\Comic-sorting-audit-20260915-windows-x64.exe`，**12,582,443 bytes**。同目錄 `Comic sorting.exe` 已更新且 hash 相同；備份位於 `backups\before-audit-20260915-123857\Comic sorting.exe`。SHA-256：`C09465D282D5A41819261CE4911C53F0B644861A140A3E41DCEC8A0C7BD89AE9`。

執行中的舊 `Comic-sorting-ntfy-20260915-windows-x64.exe` 未中斷；目前工作完成後關閉舊版，再啟動新版本才會套用修正。未修改使用者設定、權杖、歷史資料或真實漫畫，未推送、標記版本或發布遠端 Release。
