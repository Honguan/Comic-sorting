# 2026-09-12 全專案審查與優化

## 範圍與基準

檢查起點：`926980c`，工作樹無未提交修改。基準：135 項 unittest 通過（33.415 秒）。範圍為此 Git 專案的 9 個 Python 模組、測試、README、既有改善計畫、EXE spec 與 GitHub Actions；外部 BallonsTranslator 只檢查整合邊界，不修改其模型實作或使用者漫畫。

這是 Windows 本機工具：Tkinter 主執行緒接收背景執行緒的 Queue 事件，支援掃描、章節整合、翻譯、CBZ 匯出及工作資料清理。設定與增量狀態使用 JSON，沒有資料庫、HTTP 服務或自有模型 API。翻譯及設定橋接由使用者選定的 Python 子程序執行。

主要使用流程：綁定漫畫與輸出目錄 → 掃描／搜尋／選取 → 整合或加入佇列 → 設定翻譯環境與頁數 → 逐項處理 → 驗證結果 → 匯出／清理 → 保存工作狀態。翻譯失敗會阻擋同路徑後續步驟。常用功能已有多選、重試、頁數範圍、四階段進度、用量與耗時。

| 模組 | 責任與依賴 |
| --- | --- |
| Comic sorting.py（845 行） | 主視窗、掃描、整合與手動匯出協調；最大函式為 165 行 UI 建構。 |
| translation_queue.py（592 行） | 佇列 UI、設定保存、工作與進度事件消費。 |
| queue_worker.py（319 行） | 子程序、取消監看、日誌解析、工作順序與後續動作。 |
| comic_core.py（318 行） | 共用檔案、排序、完整性、CBZ、JSON、清理。 |
| bt_settings.py（627 行） | 設定文件、型別／參照檢查、備份、非同步編輯 UI。 |
| bt_config_bridge.py / bt_run_bridge.py | 由外部 Python 執行的設定與指定頁面橋接。 |
| app_logging.py / ui_language.py | UTF-8 輪替與敏感內容遮蔽／繁中英日文字。 |

應保留：stdlib＋Tkinter、單一檔案工作、事件回報、共用檔案層、原子替換、外部翻譯環境。主視窗和設定編輯器偏大，但主要是 UI 建構；此次不拆類別、不加框架、資料庫或持久掃描快取。沒有觀察到 import 循環。

## 修改前計畫

以下依已讀取的真實呼叫關係訂定，先以回歸測試確認，再逐項實作。無足夠證據的效能或功能候選不列為必要修改。

| 項目 | 分類 | 問題與影響 | 修改方式 | 風險 | 工作量 | 優先級 |
| --- | --- | --- | --- | --- | --- | --- |
| A | 邏輯／資料保護 | exit=0＋結束標記且無 ERROR 時，未完成階段不會判失敗；既有 result 可使匯出及清理繼續。 | 在共用子程序完成判斷拒絕任何已回報但未完成的階段，保留無進度輸出的既有相容性。 | 低；原本錯誤的成功將改為失敗。 | 小 | P1 |
| B | 資料保存／恢復 | 損壞、無法讀取或根型別錯誤的 JSON 靜默成空資料；可能丟失設定／佇列或來源衝突狀態。 | 寫入流程採嚴格讀取，缺檔維持首次使用預設；壞檔明確報錯並保留，手動匯出須正常結束 UI 狀態。 | 低；壞檔須先修復。 | 中 | P1 |
| C | 檔案／並行 | CBZ 與 JSON 共用固定 .tmp 檔名，兩次寫入可能互相覆寫／刪除暫存。 | 同目錄唯一暫存、關閉後原子替換；保留失敗時舊輸出，清除本次暫存。 | 低；不承諾多程序狀態合併。 | 小 | P1 |
| D | UX／錯誤恢復 | Komga 匯出後 os.startfile 例外會打斷 poll_events，留下 busy=True。 | 依主佇列既有方式隔離開啟位置錯誤，提示路徑，完成狀態照常解除。 | 低 | 小 | P1 |
| E | 開發流程 | 目前只在 v* tag 上測試／建置，日常提交與 PR 無驗證入口。 | 新增唯讀 CI，main push 與 PR 執行同一套測試／EXE 建置。 | 低；未推送不會觸發遠端。 | 小 | P2 |
| F | 架構／維護 | 大型 UI 建構、native 私有欄位與文字日誌協定容易隨外部版本變動。 | 記錄相容性邊界；已有整合測試。暫不全拆 UI 或自創協定。 | 重設計中至高 | 中至大 | P3 |
| G | UX／恢復 | 獨立扫描／整合沒有取消；程序強制結束僅能恢復最後保存狀態。 | 維持忙碌期間禁止正常關閉，未來再加入有明確提交邊界的取消／恢復。 | 中；牽涉部分檔案操作 | 中 | P3 |
| H（依官方公告補入） | 建置依賴 | PyInstaller 6.14.1 屬 GHSA-9fxf-4qw3-ghmr 受影響範圍；以提高權限執行時有環境變數偽造風險。此 app 未要求 UAC 提權，不能直接宣稱已可利用。 | 建置升級至已修復的 6.22.2（包含 Windows junction 啟動修正），獨立環境打包與實際啟動驗證。 | 低至中；需驗證打包相容性。 | 小 | P1 |

P0：目前未證實無需特殊失敗條件就會發生的資料損壞或可利用的嚴重漏洞；不得把「未發現」解讀為不存在。

## 驗證與完成条件

1. A：真實子程序輸出未完成進度、正常退出及結束標記；已有舊結果時仍不得匯出／清理。保留成功、異常完成、取消與指定頁數測試。
2. B：損壞／錯誤根型別／無法讀取的設定或狀態檔不被覆寫；兩個匯出入口均保留既有 CBZ 與工作目錄；缺檔與 UTF-8 BOM 有明確行为。
3. C：並行寫入同一目標沒有暫存碰撞，最終內容完整；失敗仍保留原目標並無本次殘留。
4. D：模擬檔案總管拒絕開啟，使用真實 Tkinter 驗證 busy 解除且有警示。
5. E：核對官方 workflow 語義、YAML 結構與本機等效命令；無遠端寫入，遠端 CI 本身列為未執行。
6. 完整 unittest、語法編譯、diff 檢查、PyInstaller 建置；再審查整份修改。效能只記錄實測，不虛構提速。

## 結果

完成 A、B、C、D、E、H 六項；未加入執行期依賴，保留設定格式與既有 API（`load_json` 的原本寬鬆預設仍保留，實際會覆寫資料的入口明確使用 `strict=True`）。

| 項目 | Root cause 與完成的修改 | 驗證證據 |
| --- | --- | --- |
| A | `run_translation` 的成功分支漏掉已回報階段未完成的條件。`queue_worker.py` 加入共用檢查；`tests/test_queue_worker.py` 保護後续匯出／清理，並更新既有解析測試與 `tests/test_bt_usage.py` 的未完成輸出預期。 | 修正前真實子程序回報 Translation 50%、exit=0、結束標記後，兩項工作均 done；修正後為 failed／blocked，原 result 與 mask 保留，無匯出。計時資料在失敗時仍可收到。 |
| B | `load_json` 一律吞掉讀取／解碼錯誤。`comic_core.py` 加嚴格讀取與 BOM 支援；主程式初始化、手動匯出、佇列匯出及翻譯器 config 均採用；手動匯出的讀取錯誤回傳終止事件，不留下背景忙碌狀態。`ui_language.py` 提供三語錯誤。 | 畸形 JSON、錯誤根型別、非法編碼與 PermissionError 都被拒絕；初次缺檔及 BOM 正常。手動／佇列兩個匯出入口均不覆寫壞狀態／原 CBZ、不清理 mask。壞翻譯器 config 不啟動子程序。 |
| C | 固定 `.tmp` 檔名讓獨立寫入共用同一暫存。`comic_core.py` 改為 stdlib `mkstemp`，仍在同目錄做 `os.replace`。 | 兩個執行緒用 barrier 強制交錯，旧版出現 WinError 32；修正後 JSON／CBZ 都保持完整，既有同名 `.tmp` 哨兵不被碰觸。寫入失敗測試確認舊檔／備份保留，且本次隨機暫存清除。 |
| D | 手動匯出的 `os.startfile` 例外跨出事件處理器。`Comic sorting.py` 沿用佇列的 OSError 隔離、日誌與位置提示。 | 真實 Tkinter 工作流程模擬 Explorer unavailable，舊版 poll 拋例外；修正後顯示一次含路徑警示，`manga_busy=False`，不把已完成輸出改為失敗。 |
| E | 只有標籤發版才驗證。新增 `.github/workflows/verify.yml`，在 main push／PR 以 contents: read 執行測試與建置。 | 遠端 HEAD 唯讀確認 default branch 為 main；YAML 解析與觸發／權限結構檢查通過；本機執行等效命令。未推送，故 GitHub 上的 CI 尚未執行。 |
| H | 已知上游 bootloader 環境信任問題。`.github/workflows/release.yml`、Verify 與 README 統一使用 6.22.2。 | 在獨立 venv 安裝並建置成功；新 EXE 實際啟動有可回應視窗，無 application_failed／ui_callback_failed。manifest 為 asInvoker。 |

額外修改文件：README 的 JSON 恢復、唯一暫存與單實例限制、建置安裝指令及本報告入口；本報告保留修改前計畫。測試變更集中於 `test_comic_sorting.py`、`test_queue_worker.py`、`test_ui_workflow.py`、`test_bt_usage.py`、`test_bt_settings.py`。

## 20 個面向逐項審查

| 面向 | 已檢查的內容／判斷 |
| --- | --- |
| 1 架構 | 沿主視窗 → 檔案層／佇列 → 外部橋接追蹤呼叫。無資料層循環；主 UI 偏大但拆分沒有本輪可驗證收益。維持現有分工。 |
| 2 程式品質 | 檢查重複、長函式、例外與副作用。原子寫入的兩個入口直接修正，未為數行建立通用交易抽象；主模組的共用函式匯入亦是既有測試／介面，不以清理名義移除。 |
| 3 正確性 | 優先檢查頁數、stage 完成、已有輸出、空來源、重疊章節、同名輸出、損壞 JSON、部分失敗。A/B 為新增修正；既有邊界測試保留。 |
| 4 效能 | 檔案掃描與 CBZ 是主要 I/O；圖片不重新編碼，ZIP_STORED 避免重複壓縮。掃描在背景、搜尋有 debounce、增量 CBZ 只查 metadata。實測見下表，沒有採用微優化／永久快取。 |
| 5 UI/UX | 多選、搜尋／清除、雙擊加入、排序、重試、空狀態、四進度、耗時、用量、忙碌控制及三語小視窗均有測試。D 解決匯出成功卻卡住的實際分支；不為增加按鈕而加功能。 |
| 6 使用方式 | EXE 無外部壓縮工具需求；首次路徑留白、Python 自動偵測、型別化設定編輯器已存在。補足建置工具安裝與壞 JSON 的恢復說明。 |
| 7 功能設計 | 已有 Queue／Retry／指定頁數／結果開啟／增量匯出。不再新增重複工作流；持久用量歷史、Resume 與更多預設組合列為未來產品取捨。 |
| 8 穩定性 | 子程序取消、watcher join、檔案替換失敗、設定衝突、備份、程式重啟、磁碟／權限失敗路徑已檢查。B/C/D 增強錯誤恢復；沒有實際斷電或填滿使用者磁碟。 |
| 9 Error handling | 子程序失敗含 log 路徑與有界尾端、設定橋接隱去可能含金鑰的原生輸出。修正 B 的吞錯及 D 的未捕捉 OSError；無新增通用錯誤框架。 |
| 10 Logging | 10 MB × 4 檔輪替、有 batch/job ID、命令／exit／進度／用量與敏感字串遮蔽。新增讀取失敗與開啟位置失敗脈絡；避免記錄壞 JSON 內容。原生 log 可包含漫畫文字與路徑，無法承諾所有任意格式秘密都被識別。 |
| 11 安全性 | 檢查連結／junction 清理邊界、來源／輸出包含、argv 傳參、暫存檔、金鑰遮蔽／備份及上游公告。程式無 shell=True、eval、exec；沒有提交設定或日誌。沒有 HTTP 入口，SQLi/XSS/CSRF/SSRF 不適用本程式。使用者選定的 Python／BallonsTranslator 屬可信執行碼；不是沙箱。 |
| 12 Concurrency | UI 僅由主執行緒更新；工作事件串接，單次背景檔案工作。C 解決暫存碰撞；沒有實作跨 EXE 的 queue/state transaction，見技術債。取消監看有現成真實程序測試。 |
| 13 資料庫／模型 | 無 DB／SQL／索引／migration。JSON 存 Job、路徑、選項與來源指紋；新可選參數不改格式，B 保護壞檔。 |
| 14 API | 無本專案的 Web API。整合面為 Python argv、頁面 manifest、JSON stdin/stdout、日誌事件；檢查 UTF-8、逗號路徑、native 設定 timeout、返回錯誤與 pages_to_process 支援。模型網路重試／rate limit 屬外部翻譯器。 |
| 15 Dependency | 主程式僅 stdlib，打包需要 PyInstaller。H 根據安全公告升級，沒有因版本新就換架構。記錄建置工具與 transitive 版本；此輪不是所有 OS／Python／外部模型套件的完整 CVE 稽核。 |
| 16 Build/Deploy | spec 包含 icon 與兩個 bridge；本機與 CI 同 Python 3.10、同 PyInstaller pin。E 補 main/PR gate；保留 v* Release 流程，無未授權遠端發佈。 |
| 17 測試 | 基準 135 → 141 項，增加的是四項已重現失敗模式與相關入口，不追求 coverage 百分比；既有資料保護、取消、三語布局、真實子程序與 GUI 測試仍通過。 |
| 18 文件 | README 與現有操作一致；舊 IMPROVEMENT_PLAN 保留歷史日期／80 項結果，不冒充本輪結果。新增本報告、恢復說明與建置指令。 |
| 19 可維護性 | 共用正確性檢查仍集中於 core／worker；新增失敗訊號與可執行測試，便於定位。外部 native 私有 schema 與字串日誌是最大演進風險。 |
| 20 技術債 | 明確記錄成因、目前／未來影響、成本與風險，未用廣泛重寫或抽象掩蓋，詳見後續表。 |

## 效能與驗證數據

| 檢查 | Before → After／結果 | 解讀 |
| --- | --- | --- |
| 未完成的正常退出 | done＋後續 cleanup → failed＋blocked | 直接重現並修正錯誤成功，非效能數字。 |
| 同目標交錯寫入 | WinError 32 → 兩個 JSON／CBZ 寫入均成功 | 僅保證完整檔案及暫存隔離，最後一次替換生效，不等於狀態合併。 |
| 合成掃描 | 0.1458 秒 → 0.1428 秒（各 5 次中位數） | 100 章、2,000 原圖＋2,000 result、各 1 KiB、本機 temp；兩版本交錯量測且輸出章數／可匯出數一致。小差異屬量測波動，不宣稱加速；未模擬 D: 磁碟／960 頁 API 工作。 |
| 完整測試 | 135 → 141，全部通過 | 最終使用獨立 Python 3.10 建置環境，26.430 秒。基準 33.415 秒不與它比較效能，因測試數與執行負載不同。 |
| 建置 | PyInstaller 6.22.2 成功，EXE 10,296,020 bytes | 新 EXE 在獨立空設定目錄啟動、存在可回應視窗、未記錄啟動例外；完成檢查後只終止該測試程序樹。 |
| 靜態檢查 | 所有專案／測試 Python 語法編譯、YAML 結構、git diff --check 通過 | 專案原本沒有 lint/type-check 設定；未宣稱執行不存在的 lint／型別 gate，未為此次新增框架。 |

診斷輸出在不入版控的 `build/audit-*.log`、`build/audit-benchmark.json`；實際啟動證據在 `build/audit-smoke-445a4a9807c84294bf47574b5d2eb49e/logs/comic-sorting.log`。測試均使用暫存資料；沒有對使用者漫畫執行翻譯、整合或清理，也沒有呼叫模型 API。另從 EXE archive 取出兩個 bridge，逐位元組確認與目前原始碼一致。

獨立建置環境：Python 3.10.6、PyInstaller 6.22.2、pyinstaller-hooks-contrib 2026.7、altgraph 0.17.5、packaging 26.3、pefile 2024.8.26、pywin32-ctypes 0.2.3。未修改使用者的全域 Python 套件或 BallonsTranslator 環境。

## 尚未修改的技術債與後續建議

| 優先 | 問題／原因 | 目前影響 → 未來影響 | 成本／風險 | 本輪不修改的理由與建議 |
| --- | --- | --- | --- | --- |
| P2 | 多 EXE 共享設定／狀態，無鎖或版本交易 | 同時寫入時最後保存的完整快照勝出，整合暫存／備份也仍按輸出命名 → 多程序操作相同資料可能失去更新。 | 中／中 | C 不是跨程序協作方案；目前單實例串行使用有效。若要支援同時開多個 EXE，優先規範單實例鎖或 scoped 路徑鎖，連同整合及狀態一致性一起驗證。 |
| P2 | 完整性與增量使用檔名、大小、mtime | 無法驗證 OCR／譯文品質；保留相同 metadata 的內容替換可能漏判 → 高完整性稽核需求需內容 hash。 | 中／I/O 成本 | 現有快速增量是明確行為；全檔 hash 會增加磁碟讀取，不在沒有需求下強制加入。 |
| P2 | 對外版本協定依賴私有欄位與文字 log | upstream 更名可使欄位或進度失效 → 更新 BallonsTranslator 時需相容測試。 | 中／中 | native 沒有本專案可控制的穩定 IPC；保留真實 bridge 及子程序測試，未自創一套 native API。 |
| P2 | Python 3.10 建置基底在本機為 3.10.6 | 舊執行期不是安全更新完整的環境 → 應建立較新 Python 的測試與打包矩陣。 | 中／中 | 此輪只升級有已確認公告的 PyInstaller，保留專案既有 Python 3.10 相容需求；不宣稱 bundled interpreter／所有傳遞依賴都已全面修復。 |
| P2 | 掃描的 os.walk 忽略部分目錄讀取錯誤；單章消失可使本次掃描失敗 | 不可讀目录可能未呈現 → 若支援離線磁碟／不穩定 NAS，需部分結果＋錯誤摘要。 | 中／低至中 | 尚未有該資料環境的症狀；單純吞更多錯誤會隱藏漏掃，應明確設計部分結果狀態再加。 |
| P3 | 無硬性翻譯 timeout、未定義外部多程序子工作取消 | 模型等待可很長；目前可停止主翻譯程序 → 外部新增子工作時需整棵程序生命週期管理。 | 中／中 | 任意 timeout 會中止合法長 OCR；不在無模型時序證據下加硬限制。 |
| P3 | 無獨立掃描／整合取消與完整 crash journal | 正常關閉會要求等候；強制結束只能恢復最後保存快照，可能留下暫存 → 更長批次可受益於恢復設計。 | 中至大／資料安全 | 必須定義檔案提交邊界、清理與恢復，一個 stop flag 不能完成；未用半成品取消冒充完整能力。 |
| P3 | 用量／時間只有當批、UI 類別較大 | 重啟後沒有成本歷史；閱讀建構函式較長 → 需更多預設／報表時再拆。 | 中／低至中 | 已滿足目前操作；無需資料庫、歷史報表、平行翻譯或全面 UI rewrite。 |

優先後續：建立新 Python／實際 BallonsTranslator 的打包相容矩陣，其次才是同路徑多實例與可取消掃描。其餘保留目前簡單方案。

## 官方依據與再審查

- [GHSA-9fxf-4qw3-ghmr](https://github.com/pyinstaller/pyinstaller/security/advisories/GHSA-9fxf-4qw3-ghmr)：<6.22.1 的 onefile 環境偽造問題；提權影響需要 privileged executable 條件。本產物 manifest 驗證為 asInvoker，仍升級建置工具避免留下已知問題。
- [PyInstaller 6.22.2 changelog](https://pyinstaller.org/en/stable/CHANGES.html)：修正 Windows symlink／junction 目錄啟動時誤觸安全驗證。
- [先前 Windows 公告](https://github.com/pyinstaller/pyinstaller/security/advisories/GHSA-9w2p-rh8c-v9g5) 的修復線為 5.13.1；[bootstrap 公告](https://github.com/pyinstaller/pyinstaller/security/advisories/GHSA-p2xp-xx3r-mffc) 影響 <6.0.0。沒有把所有舊公告都算成此程式的新漏洞。
- [外部程序 DLL 搜尋路徑說明](https://github.com/pyinstaller/pyinstaller/blob/v6.14.1/doc/common-issues-and-pitfalls.rst)：Windows DLL 搜尋路徑可能繼承；目前的 bridge 已避免把 `_MEIPASS` 插入外部 Python 的 import path。沒有在未重現其他衝突時全域改動 Tk 的 DLL 搜尋設定；實際模型啟動仍屬相容矩陣的未驗證部分。
- [GitHub Actions triggers](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows)：main push／PR 為新增檢查入口；官方文件經 Context7 查證。

再審查已核對新增條件位於共用 worker、所有會更新 JSON 的呼叫者、兩條匯出錯誤退出路徑、唯一暫存的失敗清理、原函式呼叫相容性，以及 YAML／README 版本一致性。完整測試與打包通過；不把單元／合成測試延伸為真實模型品質、斷電安全、全面 CVE 或遠端 CI 成功的證明。
