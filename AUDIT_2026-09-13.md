# 2026-09-13 全專案檢查與改善

## 範圍與現況

基準提交 `9f2d9c6346db80c2ab6ea45144a77267456af6a6`，包含本次開始前尚未提交的摺疊、系列排序、視窗握把與右鍵移除功能。審查命令為 `git diff HEAD`；原始工作樹快照保存在不入版控的 `build/audit-2026-09-13-before/`，不把這些先前功能當成本輪新修正。

檢查範圍：9 個 Python 模組、11 個測試模組、README、歷史改善文件、PyInstaller spec 與兩個 GitHub Actions。這是供本機漫畫整理、翻譯及 Komga 匯出使用者操作的 Windows／Tkinter 工具；主執行緒管理 UI，背景執行緒以 Queue 回報，外部 Python 執行 BallonsTranslator。設定及 Komga 增量資訊使用 JSON，沒有資料庫、HTTP 服務或本專案直接發送的模型 API。

流程：選擇漫畫目錄 → 背景掃描 → 搜尋／排序／選取 → 整合或加入佇列 → 檢查原生設定與頁數 → 串行翻譯／匯出／清理 → 驗證結果、保存狀態。原生翻譯器的模型品質、帳戶與並行策略是整合邊界，本輪不修改外部專案或使用者漫畫。

| 模組 | 基準行數 | 責任 |
| --- | ---: | --- |
| Comic sorting.py | 914 | 主視窗、掃描、選取、整合、手動匯出及事件處理 |
| translation_queue.py | 637 | 主佇列 UI、工作設定、進度／成本／時間及狀態保存 |
| comic_core.py | 327 | 圖片、完整性、CBZ、JSON、路徑及清理 |
| queue_worker.py | 319 | 子程序啟動／取消、日誌協定、串行工作與後續動作 |
| bt_settings.py | 627 | 型別化設定編輯、原生驗證、備份及衝突偵測 |
| bt_config_bridge.py / bt_run_bridge.py | 177 / 42 | 外部環境的設定定義／原生頁面選取橋接 |
| app_logging.py / ui_language.py | 45 / 346 | UTF-8 輪替／遮蔽及三語文字 |

優點：stdlib 執行期、UI 與 worker 分離、失敗阻擋後續破壞性操作、設定衝突保護、來源／輸出驗證與實際 Tk／子程序測試。無本地 import 循環。最大維護風險是外部私有 schema／文字日誌協定，以及多 EXE 共用設定卻沒有交易協作；UI 建構函式較長但不因此全面拆分。

## 修改前優先計畫

下列計畫在任何本輪產品程式修改之前建立，逐項補可失敗的驗證後再實作。

| 項目 | 分類 | 問題 | 影響 | 修改方式 | 風險 | 工作量 | 優先級 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A | 穩定性／檔案並行 | JSON／CBZ 原子替換遇到 Windows 短暫開啟中的目標即失敗；受控開檔得到 WinError 5，20 次交錯寫入有 11 次錯誤。 | 設定保存、批次及 CI 間歇失敗。 | 共用檔案替換增加有上限的短暫 Windows 鎖重試；永久錯誤仍拋出、舊檔與暫存清理保持。 | 低；受阻時最多增加短暫等待，非多程序合併方案。 | 小 | P1 |
| B | 安全／可觀測性 | 遮蔽 regex 的單字邊界漏掉 OPENAI_API_KEY 等底線前綴。 | 原生輸出／例外若含此類欄位，可能記錄秘密。 | 匹配完整憑證欄位名，保留既有 Bearer、JSON、URL 格式；只用合成 sentinel 驗證。 | 低；檢查正常用量欄位不被遮蔽。 | 小 | P1 |
| C | 邏輯／UX | 套用系列排序重建選取，手填整合範圍 2–3 變為 1–4。 | 改變使用者待執行的整合範圍。 | 重用同一排序計算，僅移動既有系列列，不重設選取事件或整合欄位。 | 低；需檢查篩選後序號、選取、升降冪與重啟。 | 小 | P2 |
| D | 設定／UX | require_api_key 布林欄位被 secret_path 判定為金鑰。 | 出現密碼框，無法正常開關，清空則型別錯誤。 | 精確排除能力開關的名字，真正 api_key 及其內部結構仍遮蔽。 | 低；檢查布林保存與金鑰保護。 | 小 | P2 |
| E | 相容性／程序生命週期 | 取消只終止主 PID；繼承 stdout 的子程序會延後 pipe EOF。 | 外部程序若留下子工作，停止可能等待其結束。 | 保留現有主程序取消；未來以真實原生子工作規格驗證 Windows Job Object 等整棵生命週期方案。 | 中至高；不能誤殺使用者其他 Codex／Python。 | 中 | P3 |
| F | 多實例／恢復 | 多 EXE 同一設定的最後快照覆蓋較早快照，整合中間目錄名固定。 | 多實例資料可能失去更新；強制中止只有最後已保存狀態。 | 維持單實例使用約定，後續一起設計單實例鎖與恢復提交邊界。 | 中；涉及操作方式與檔案生命週期。 | 中至大 | P3 |

P0：本輪尚無已證實需緊急修正的 P0；這不表示不存在其他漏洞或資料風險。

### 驗收條件

1. A：真實 Windows 讀取鎖短暫釋放後 JSON／CBZ 均成功；鎖不釋放與一般權限錯誤有界失敗並保留原檔；重跑原本 20 次交錯寫入。
2. B：日誌訊息、例外與 worker 尾端共用遮蔽，帶服務前綴的合成秘密不可出現在輸出，正常 token 計數保留。
3. C：真 Tk 選父系列、手填 2–3、套用排序與執行選取事件後，範圍仍是 2–3，整合確認僅含指定兩章。
4. D：真 Tk 呈現 require_api_key 核取方塊，可套用、保存及重新載入；api_key 仍用隱碼欄位。
5. 最後完整 unittest、語法及 diff 檢查、EXE 建置、修改後再審查。沒有現成 lint／type-check 設定，不引入無關框架充數。

## 執行結果

完成 A／B／C／D，共 4 項；E／F 保留。每項先執行會失敗的回歸測試，修改後再通過相關測試，最後完整測試及建置。沒有修改設定格式、加入依賴、執行真實翻譯或變更使用者漫畫。

### A：Windows 短暫檔案鎖（P1，已修正）

- 根因：唯一暫存檔只能避免暫存檔名稱碰撞；替換目標仍可能被讀取中的 handle 或待刪除狀態阻擋。受控讀取鎖測試在原碼的 JSON 與 CBZ 路徑都重現 WinError 5；沒有證據把原因指定為防毒軟體或永久 ACL 問題。
- 修改：`comic_core._replace_file` 共用於 `save_json`、`create_cbz`；僅 Windows 錯誤碼 5／32／33 重試，最多 6 次嘗試，等待 20／40／80／160／320 ms，合計 0.62 秒。正常寫入不等待；其他錯誤直接拋出。目錄整合的替換流程未改動。
- 效果：原本 20 次雙執行緒交錯寫入有 11 次錯誤，修改後 20／20 通過。短暫讀取鎖 80 ms 後釋放，JSON 與 CBZ 都能完成。
- 失敗保護：模擬永久錯誤驗證重試上限與一般錯誤不重試；另以真實持續讀取鎖驗證 JSON／CBZ 均約 0.67 秒回報 WinError 5，原檔保留、零暫存檔殘留。重試時間上限指主動等待，實際 I/O 與排程仍占時間。
- 風險：持續占用／權限不足仍失敗；原子性不等於多實例交易，也不保證斷電後磁碟已落盤。

Windows 官方文件說明，是否允許後續刪除／改名取決於 `FILE_SHARE_DELETE`，分享模式衝突及待刪除檔案會造成開啟失敗。這支持短暫 handle 衝突的診斷，但不能判定是何外部程式占用。[Microsoft CreateFileW](https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-createfilew)

### B：帶前綴的憑證遮蔽（P1，已修正）

- 根因：`api_key` 前的 regex 單字邊界無法匹配 `OPENAI_API_KEY`，因底線仍屬於單字字元。
- 修改：在既有 `app_logging.redact` 支援以底線／連字號分隔的欄位前綴；日誌 Formatter、翻譯器輸出與錯誤尾端繼續共用此函式。
- 驗證：合成子程序輸出 `OPENAI_API_KEY`、`OPEN_ROUTER_ACCESS_TOKEN`，JSON 日誌含 `service-api-key`；修正前秘密出現在錯誤字串，修正後日誌與錯誤皆無 sentinel，`total_tokens` 保留。既有 Bearer／密碼／例外及輪替測試通過，共 5 項。
- 限制：這是已知憑證格式的遮蔽，不能保證辨識自由文字中的所有敏感內容；本輪未讀取或外傳真實金鑰。

### C：排序保留手動整合範圍（P2，已修正）

- 根因：排序設定套用時呼叫 `apply_scan_data` 重建 Treeview，重建後的 `on_tree_select` 及選取事件將手填 2–3 重設為父系列全部 1–4。
- 修改：抽出既有排序計算 `sorted_series` 給初次掃描與排序共用；套用時只移動父系列列、更新序號，保留章節列及選取。搜尋／重新掃描仍沿用原有流程。
- 驗證：真 Tk 選父系列、手填 2–3、套用排序並處理事件後，整合確認只含第 2／3 章。全部 8 種欄位／方向、搜尋、重啟、保存失敗及選取／展開狀態通過；補充隔離檢查含「. 」名稱、篩選序號、焦點、多選欄位停用也通過。
- 測試調整：既有排序測試在程式化選取後加入 `root.update()`，先送出真實 Tk 選取事件，不再依賴排序重建的副作用。
- 效果：保留使用者輸入，並減少重建章節 Widget 的成本；量測見下節。

### D：「需要 API 金鑰」開關（P2，已修正）

- 根因：`secret_path` 使用欄位名稱子字串判斷，將 `require_api_key` 布林值當成金鑰，顯示隱碼輸入框；清空後又不符合原生布林型別。
- 修改：僅將精確欄位名稱 `require_api_key` 排除於秘密名稱判定，沿用現有布林核取編輯器。若上層是 `api_key`，仍視為秘密。
- 驗證：真 Tk 開關保存、重新載入為 `False`，原金鑰保持原值且 UI 隱碼；設定文件、橋接及編輯器 19 項測試全部通過。

## 逐項審查結論

| 面向 | 檢查與結論 | 本輪處置／保留理由 |
| --- | --- | --- |
| 1. 架構 | 主視窗與佇列 UI 協調 worker；共用檔案操作集中於 core，無本地 import 循環。 | 保留架構；只抽出兩個確有兩處使用的替換／排序計算，未新增服務層。 |
| 2. 程式碼品質 | UI 建構函式較長；翻譯器啟動／工作函式約百行，含生命週期與錯誤處理。 | 長度不足以證明拆分收益；不全面拆 God Class，不刪除未證明無用的介面。 |
| 3. 邏輯正確性 | 查空集合、重複／重疊章節、頁面範圍、Decimal 編號、部分失敗及狀態保存。 | 修正排序副作用；既有整合、CBZ、頁數、用量合計回歸通過。 |
| 4. 效能 | 掃描／匯出已有背景執行與搜尋 debounce；CBZ 使用 ZIP_STORED。排序原會重建所有顯示章節。 | 修正排序路徑並實測；未憑空增加快取或翻譯並行數。 |
| 5. UI／UX | 檢查搜尋、排序、多選、確認、忙碌防護、摺疊、視窗大小、右鍵移除及長任務回饋。 | 修正手動範圍及布林欄位；最小視窗／三語／既有佇列功能測試通過。 |
| 6. 使用方式 | README 有下載、操作及建置指令；外部 Python 可自動尋找，也能手動指定。 | 更新四項行為說明；保留原生設定入口，不另建重複設定精靈。 |
| 7. 功能設計 | 已有佇列、重試、頁數範圍、增量匯出、進度、用量與耗時。 | 不為數量新增功能；較有價值的未來工作是多實例保護與中斷恢復。 |
| 8. 穩定性 | 唯一暫存檔、CBZ 驗證、原生設定備份／衝突檢查已有測試；替換仍遇短暫鎖。 | 修正 A；磁碟滿／永久鎖以保留原檔及清楚失敗處理，不吞掉例外。 |
| 9. 錯誤處理 | 主 UI callback 記錄 traceback；worker 保留上下文、近期輸出與失敗項目；失敗阻擋後續清理。 | 共用遮蔽修復涵蓋錯誤詳情；未把寬泛 catch 一律刪除，UI／worker 邊界需要隔離失敗。 |
| 10. 日誌／可觀測性 | UTF-8、10 MB 輪替、3 份備份；工作上下文、PID、退出碼、用量與時間已有回報。 | 修正 B；未另加無使用者需求的 tracing／metrics 服務。 |
| 11. 安全性 | 檢查命令參數、JSON、路徑包含／junction、清理與暫存；本機沒有 Web／SQL 表面。 | 修正憑證格式漏洞；保留信任的原生 Python 邊界。SQL injection／XSS／CSRF 無適用入口。 |
| 12. 並行／取消 | 每次佇列串行，worker 以事件回報；JSON／CBZ 替換存在平台鎖競爭，取消僅主 PID。 | A 已修；E／F 待處理。未新增全域鎖或提高原生 Codex 並行度。 |
| 13. 資料模型 | 無資料庫。JSON 保存設定／工作快照／增量狀態；壞檔嚴格載入、未知原生設定保留。 | 保留格式，沒有 SQL／index／N+1 調整；多實例最後寫入覆蓋屬 F。 |
| 14. API／相容性 | 此專案的 API 邊界是外部 CLI、設定 schema 與文字日誌解析；API 驗證由原生環境執行。 | 已測 malformed／缺失回報、重試與失敗摘要；私有 schema 升級仍需相容性樣本。 |
| 15. 依賴 | 執行期僅 stdlib；PyInstaller 限於建置，README 與兩個 workflow 固定 6.22.2。 | 核對官方安全／版本資訊後保留版本；未替外部 BallonsTranslator 依賴做完整漏洞認證。 |
| 16. 開發／部署 | Verify 只讀並測試／建置；Release 在 v* tag 後測試、建置及發布。 | 本機已使用同版建置器；未推送、建立標籤或發布 Release。 |
| 17. 測試 | 既有實際 Tk、子程序、Windows junction、檔案與資料完整性測試。 | 增加 5 項行為回歸，145 → 150；全部通過，不新增測試框架。 |
| 18. 文件 | README 覆蓋基本使用、設定、日誌、清理與開發；歷史報告保留脈絡。 | 更新 README 並新增此報告，明確區分本輪／先前功能與未驗證項目。 |
| 19. 可維護性 | core／worker 可獨立測；原生私有結構及字串協定是主要變動來源。 | 局部修正、保留命名／風格；將相容性樣本列入後續優先建議。 |
| 20. 技術債 | 多實例寫入、程序樹取消、崩潰恢復、外部協定及本機／CI Python patch 差異。 | 見下方成本／風險表；沒有把全部技術債包進本輪變更。 |

PyInstaller 官方公告指出先前 bootloader 環境變數安全問題於 6.22.1 修補；6.22.2 另修正 Windows junction／symlink 啟動檢查。本專案已固定 6.22.2，沒有理由只因存在版本差異就修改建置鏈。[官方安全公告](https://github.com/pyinstaller/pyinstaller/security/advisories/GHSA-9fxf-4qw3-ghmr)、[官方更新記錄](https://pyinstaller.org/en/stable/CHANGES.html)

## 實測與完整驗證

環境：Windows 10.0.26100、Python 3.10.6、Tkinter、PyInstaller 6.22.2。數字均為本機量測，不推論其他磁碟／硬體或模型 API 效能。

| 項目 | 修改前 | 修改後 | 方法與限制 |
| --- | ---: | ---: | --- |
| 平行原子寫入 | 9／20 通過，11 次錯誤 | 20／20 通過 | 相同既有 unittest 重複 20 次；兩執行緒交錯 JSON／CBZ，確認完整內容及無關暫存檔。這是穩定性樣本，不是生產失敗率預估。 |
| 套用系列排序中位時間 | 37.48 ms | 5.79 ms | 合成 60 系列／1,200 章；1 次暖機、7 次量測，交替名稱升降冪；Tk 根視窗隱藏，包含套用及事件處理，排除存檔 I/O／對話框建立／真實漫畫掃描。約減少 85%。 |
| 完整測試 | 起始既有記錄 144／145 通過（WinError 5） | 150／150，35.810 秒 | 本輪先重現四項問題，最終 `py -3.10 -B -m unittest discover -s tests -v`。測試數與環境負載不同，不以總測試秒數聲稱程式效能提升。 |
| 語法與差異 | — | 20 個 Python 檔案編譯檢查通過；`git diff --check` 通過 | 沒有現成 lint／type-check 設定；未為這次審查加入框架。 |
| 建置與啟動 | — | EXE 建置成功；隔離啟動檢查通過 | 建置器 6.22.2；獨立 build 資料夾、空設定，執行 3 秒確認子程序及 application_start、沒有失敗日誌，再結束測試程序。未操作真實翻譯。 |

排序原始樣本（毫秒）：

- 修改前：40.18、37.38、35.54、40.23、37.95、37.40、37.48。
- 修改後：4.82、4.89、6.29、6.76、5.79、5.70、6.79。

本機重現證據保存在忽略版控的 `build/`：`audit-2026-09-13-before/`、`audit-only.diff`、`audit-2026-09-13-baseline-writes.txt`、`audit-2026-09-13-after-writes.txt`、`audit-persistent-lock.json`、`audit-full-tests.log`、`audit-build.log`、`audit-sort-before.json`／`audit-sort-after.json`、`audit_ui_benchmark.py`。未把日誌、快照、EXE 或測試環境加入版本控制。

## 修改後重審

兩條獨立唯讀重審路徑分別檢查 core／遮蔽／設定與 UI／排序，皆以本輪前的工作樹快照作基準，未混入先前功能。沒有發現新的阻擋項目；確認替換有界且不吞永久錯誤、上層秘密仍遮蔽、排序不再改變整合範圍。既有 145 項功能加新增 5 項回歸完整通過，沒有藉修復重寫架構、改設定格式或增加執行期依賴。

## 尚未修改與後續優先建議

| 項目 | 原因／現有影響 | 後續方式、成本與風險 |
| --- | --- | --- |
| E，P3：取消整個程序樹 | 合成子程序繼承 stdout 時，停止主 PID 後約 3.16 秒才隨子程序結束返回；真實 BallonsTranslator／Codex 子工作生命週期尚未完整驗證。 | 中成本、中高風險。先定義本工具擁有的程序樹邊界，再評估 Windows Job Object。不能直接按名稱終止所有 Python／Codex。 |
| F，P3：多實例與恢復 | 共用 JSON 是最後快照寫入勝出；整合暫存／備份目錄名固定。現有 README 要求一次使用一個實例，任務恢復取決於最後已保存快照。 | 中至大成本、中風險。若要支援多視窗／當機續跑，先加單實例協調或交易邊界，再設計明確恢復狀態；單純重試不解決此需求。 |
| G，P3：外部協定相容性 | 原生私有 schema、JSON 及文字日誌可能隨 BallonsTranslator 變動；OCR／翻譯 token、時間和金額依原生回報，並非帳單。 | 中成本。以已支援原生版本的匿名樣本建立契約測試；原生升級時測設定往返、頁數、失敗／成功／取消，不盲目加入版本猜測分支。 |
| H，P3：環境可重現性 | 本機為 Python 3.10.6，CI 指定 3.10 大小版本，由 setup-python 選 patch；建置 hooks 也可能不同。 | 小至中成本。下一次發版用乾淨受控環境核對 Python／Tk／hooks，升級後跑現有 Windows 測試。此次沒有修改使用者 Python 或外部依賴。 |
| 保留：大型 UI 拆分／新快取／增加翻譯並行 | 尚無量測證明拆層、快取或提高並行能改善目前瓶頸；API 執行在外部專案。 | 出現真實延遲／容量瓶頸後再量測；避免用更複雜的架構替代現有可測模組。 |

本輪沒有進行真實付費 OCR／翻譯、大規模 D:\Mangas 掃描、斷電測試、所有 Windows／原生版本相容性驗證，也沒有承諾消除全部安全風險。這些限制不影響上述四項已重現、已回歸驗證的修正。

## 修改檔案與交付

| 本輪檔案 | 內容 |
| --- | --- |
| [comic_core.py](<comic_core.py>) | JSON／CBZ 共用 Windows 替換重試。 |
| [app_logging.py](<app_logging.py>) | 帶前綴憑證名稱遮蔽。 |
| [Comic sorting.py](<Comic sorting.py>) | 共用排序計算、移動既有父列保留範圍。 |
| [bt_settings.py](<bt_settings.py>) | require_api_key 布林開關分類。 |
| [tests/test_comic_core.py](<tests/test_comic_core.py>) | 短暫鎖／有界錯誤與原檔保護。 |
| [tests/test_app_logging.py](<tests/test_app_logging.py>) | 合成子程序／日誌憑證回歸。 |
| [tests/test_ui_workflow.py](<tests/test_ui_workflow.py>) | 手填整合範圍回歸、送出 Tk 選取事件。 |
| [tests/test_bt_settings.py](<tests/test_bt_settings.py>) | 布林開關保存／重載與金鑰保護。 |
| [README.md](<README.md>)、本報告 | 行為、使用限制與本次證據。 |

可執行檔：`Comic sorting-優化版.exe`（本次審查時交付的本機版本），10,298,376 bytes。與 `dist\Comic sorting.exe` SHA-256 一致：`9694EE7C53D0A9897AEF004298D1D4561BED46A4CB7C850D23646978D70E26D3`。使用時先關閉舊版，避免同時操作相同設定與漫畫。

先前未提交的摺疊、排序設定、視窗調整、右鍵移除等變更保留；本輪未建立提交、推送或發布遠端版本。
