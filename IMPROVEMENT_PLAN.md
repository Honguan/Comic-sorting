# 專案改善計畫

依據：2026-09-08 工作樹、整合／掃描／匯出／佇列的呼叫流程與既有測試。先訂定以下範圍與驗收條件，再開始修改；結果補在本文末尾。

## 優先順序與驗收

| 優先 | 面向 | 已發現問題 | 修正方式與驗收條件 |
| --- | --- | --- | --- |
| P0 | 整合正確性 | 既有合併輸出被排除於來源；原章節已清除時，重整可能只留下最後一章。合併資料夾只取第一個章號，續接章節時範圍名稱也可能錯誤。 | 辨識合併範圍；拒絕重疊來源，明確提示改選不重疊章節。續接完整合併來源，正確命名，失敗保留所有來源及既有輸出。 |
| P0 | 匯出正確性 | 有任意 result 圖片就標成可匯出；不同上層目錄的同名系列會寫入同一 CBZ。 | 共用翻譯完整性判斷；部分翻譯不可匯出／自動清理；已記錄其他來源的同名輸出拒絕覆蓋。保留只有 result 的既有使用方式。 |
| P0 | 增量匯出 | 只比較來源指紋，損毀／被外部改動的 CBZ 仍可能被跳過並觸發清理。 | 記錄輸出大小與修改時間；輸出變更時重建，舊狀態先驗證再採用；正常未變更 CBZ 不重讀內容。 |
| P1 | 佇列續跑 | 只在整批結束保存狀態，中斷後已完成工作可能重跑；原執行中工作直接恢復為等待。 | 每次收到完成／失敗等終態就保存；保存失敗停止後續工作。重啟後未確認完成的工作顯示已停止，由使用者重試。 |
| P1 | 掃描安全與效能 | 掃描會進入 mask、整合暫存／備份，Windows junction 可能造成重複或循環遍歷；空白路徑等同目前工作目錄。 | 排除工作與整合中間目錄及目錄連結；大小統計不追蹤連結；空白掃描／清理直接拒絕。以實際 junction 與目錄遍歷測試驗證。 |
| P1 | 操作手感 | 搜尋每一字都重建列表；重繪會重設捲動位置，錯誤一律跳到設定頁；忙碌時選取摘要容易過期。 | 搜尋合併短時間輸入，加入清除與搜尋快捷鍵；同一掃描資料的重繪保留捲動；依錯誤所在頁引導；刷新選取摘要。 |
| P1 | 小視窗操作（實測補入） | 820×640 時佇列開始／停止與進度控制項未映射，無法操作。 | 使用 Tkinter 原生上下分隔窗；先保留工具列與進度空間，列表使用剩餘空間。三語在最小尺寸的按鈕必須實際顯示且位於視窗內。 |
| P1 | 複選整合（續接流程補入） | 選取合併章節與下一章時，原範圍邏輯還會包含中間未選的保留章節，無法避開重疊。 | 明確複選章節時只整合選取項目並停用範圍輸入；選取父系列或單一章節時維持範圍操作。驗證跳過未選章節且不清除它。 |
| P2 | 設定與可攜性 | 匯出選項不保存；預設安裝路徑綁定開發者磁碟；手輸漫畫路徑不會在掃描時保存。 | 保存匯出選項和有效手輸路徑；新設定留空，既有設定照常載入；由已設定安裝目錄推導 config 預設。 |

## 架構決策

- 保留 Tkinter 主執行緒、背景工作事件佇列、`comic_core.py` 檔案操作與 `queue_worker.py` 程序執行的現有分工。
- 將翻譯完整性與匯出保護放在共用檔案層，讓直接匯出、主佇列與畫面狀態遵守相同規則；不新增框架、資料庫或通用工作流引擎。
- 繼續使用單一背景檔案工作，避免同路徑整合、匯出、清理同時進行。尚無證據需要平行翻譯或磁碟操作。
- 搜尋只使用已掃描資料；不做容易漏掉外部修改的持久掃描快取。先減少重複 UI 更新與不必要目錄遍歷。

## 執行與驗證

1. 為上述資料正確性問題建立會失敗的回歸測試，再修正共同入口。
2. 驗證真實子程序的路徑傳遞、Windows junction、Tkinter 搜尋／選取／重啟保存與佇列失敗隔離。
3. 比較連續搜尋的重繪次數、增量匯出的內容讀取次數；效能結論以可重現量測為準，不宣稱未量測的倍數。
4. 執行完整 unittest、三語文字檢查、Git diff 檢查及 Windows EXE 建置；更新使用文件並只提交相關原始碼與文件。
5. 不對使用者漫畫執行翻譯、整合或清理。真實模型／API 翻譯品質與大型實際磁碟吞吐量列為未驗證限制。

## 後續候選（需量測或另定產品規格）

- 獨立掃描／整合／匯出的取消按鈕與離開程式流程：需要定義取消後的暫存恢復與清理邊界，避免只中止執行緒而留下不完整結果。
- 持久化欄寬與更多快捷操作：可拖曳上下區域已因最小尺寸實測問題納入本次；其他項目需確認實際使用需求。
- 多安裝環境與翻譯預設組合：現有需求只有一個 BallonsTranslator 安裝，暫不增加設定層次。

## 執行結果

以上 9 類修正已實作，保留既有架構且未新增依賴。新增 26 項回歸測試，完整測試由 54 項增至 80 項。

| 驗收項目 | 已取得證據 |
| --- | --- |
| 重疊、續接、空來源與既有輸出 | 測試重現舊版本將 `Chapter 1-3` 重建成只剩最後一章；修正後拒絕重疊且保留翻譯。`1-3`＋`4-6` 的 6 張原圖全部進入 `1-6`；空來源不進入清理；單選既有輸出不改動翻譯。 |
| 複選與清理範圍 | 由真實 Tkinter 複選 `1-45` 與 `46`，經確認整合與背景工作建立 `1-46`；未選取的 `45` 原圖仍在。 |
| 翻譯與匯出 | 部分翻譯在掃描、直接匯出及主佇列均不可匯出；後續清理被阻擋。同名來源碰撞在共用入口及整批匯出均保留原 CBZ 與第二個來源的工作資料。 |
| 增量完整性與成本 | 損毀的新／舊狀態 CBZ 都會重建。舊狀態的完好 CBZ 只驗證 1 次；之後未變更的 CBZ 驗證與重建次數均為 0，以檔案 metadata 判定。 |
| 佇列保存 | 整批尚未結束時，設定檔已記錄第一項完成、第二項執行中；保存失敗設定停止旗標。重啟保留完成狀態，執行中項目改為已停止並附提示。 |
| 掃描 | 實際建立 Windows junction 驗證不遍歷外部目錄、不計入其容量、不採用 linked result；mask、整合暫存／備份不列為章節。空白掃描／清理不啟動工作。 |
| 搜尋與操作 | 同一組 6 次連續輸入，重繪由 6 次降為 1 次；測試也驗證查無結果的選取數歸零、忙碌時摘要更新、佇列重繪保留焦點／選取／捲動位置，以及三種錯誤引導到相應分頁。 |
| 小視窗 | 舊版在 820×640 的開始按鈕實際未映射；修正後繁中／英文／日文的開始、停止、進度、整合、匯出與設定控制項均實際顯示且位於視窗範圍內。此檢查使用 Tkinter 實際視窗幾何，非只檢查 requested size。 |
| 設定 | 有效手輸路徑在掃描前保存；重啟保留匯出選項；新安裝路徑留空；既有語言與佇列保存測試仍通過。 |

完整驗證：`py -3.10 -B -m unittest discover -s tests -v`，80 項全部通過（最後一輪 14.404 秒）。`git diff --check` 通過。

Windows 建置：`py -3.10 -m PyInstaller --noconfirm "Comic sorting.spec"` 成功，產出 `dist/Comic sorting.exe`（9,638,468 bytes）。環境中的 rapidfuzz hook 有既存警告，未阻止產出，並非本專案匯入的依賴。

修改檔案：

- `Comic sorting.py`：上下分隔窗、工具列空間、搜尋、複選與整合流程、掃描排除、設定保存。
- `comic_core.py`：章節範圍、翻譯完整性、連結判斷、匯出衝突與增量 CBZ 檢查。
- `queue_worker.py`：沿用共用翻譯完整性判斷。
- `translation_queue.py`：工作狀態保存、重啟恢復、捲動與錯誤分頁、工具列布局、可攜式設定預設。
- `ui_language.py`：新增提示的英文與日文文字。
- `tests/test_improvements.py`：26 項資料與 GUI 回歸測試。
- `tests/test_comic_sorting.py`、`tests/test_ui_workflow.py`：配合新操作規則更新既有驗證，保留其原本保護範圍。
- `README.md`、`IMPROVEMENT_PLAN.md`：操作說明、事前計畫、驗收與限制。

限制：未啟動真實漫畫翻譯／模型 API，未量測使用者大型磁碟的實際吞吐量。翻譯完整性依檔名對應，不驗證翻譯品質。增量檢查以檔案大小與修改時間作快速判定，不能偵測刻意保留相同 metadata 的內容改動；需要完整內容稽核時再加入內容雜湊。斷電／強制結束只能恢復最後成功保存的工作狀態；未確認完成的工作需檢查後重試。
